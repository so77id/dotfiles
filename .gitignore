# Antigen file
antigen.zsh

# Folder view configuration files
.DS_Store
Desktop.ini

# Thumbnail cache files
._*
Thumbs.db

# Files that might appear on external disks
.Spotlight-V100
.Trashes

*.zwc

# Claude Code local settings (per-user, per-project)
.claude/

# Claude Code: versionamos claude/ (sin punto), pero NUNCA lo local/sensible
claude/settings.local.json
claude/**/*.local.json
claude/**/*.dotfiles-bak
